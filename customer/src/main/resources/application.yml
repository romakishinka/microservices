server:
  port: 8080
spring:
  application:
    name: customer
  datasource:
    password: 2897
    url: jdbc:postgresql://localhost:5432/postres
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true